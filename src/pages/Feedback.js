import React, { Component } from 'react';

class Feedback extends Component {
  render() {
    return (
      <main>
        <h1 data-testid="settings-title">Feedback Page</h1>
      </main>
    );
  }
}

export default Feedback;
